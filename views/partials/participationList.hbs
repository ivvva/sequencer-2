<h1>USERS</h1>

<div>
    {{#each users}}
        {{username}}
        {{location}}
        {{!-- composition image_url --}}
        {{createdAt}}
        <a href="/existing-composition/{{_id}}">Composition</a>
    {{/each}}
</div>

